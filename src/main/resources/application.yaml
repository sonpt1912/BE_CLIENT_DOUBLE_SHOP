server:
  port: 8092

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/double_shop?useSSL=false
    username: root
    password: Son191203
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true